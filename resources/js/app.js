import './bootstrap';
$(document).ready(function() {
    // Code for toggling sidebar
    $('.sidebar-toggle').on('click', function(e) {
        e.preventDefault();
        // Your code to toggle sidebar
    });
});

